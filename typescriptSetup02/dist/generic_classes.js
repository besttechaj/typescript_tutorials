"use strict";
// console.log(`generic class`);
const anotherFunction = (valOne, valTwo) => {
    return { valOne, valTwo };
};
class Sellable {
    constructor() {
        this.cart = [];
    }
    addToCart(products) {
        this.cart.push();
    }
}
